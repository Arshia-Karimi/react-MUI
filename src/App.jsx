import "./App.css";
import { makeStyles } from "@mui/styles";

function App() {
  const style = useStyles();

  return <div></div>;
}

const useStyles = makeStyles((theme) => ({}));

export default App;
